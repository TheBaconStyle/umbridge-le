import { AppBar, Toolbar } from "@mui/material"

export function Footer() {
  return (
    <AppBar component="footer" position="relative" sx={{ mt: "auto" }}>
      <Toolbar>footer</Toolbar>
    </AppBar>
  )
}
